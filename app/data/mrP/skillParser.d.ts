import * as skillTypes from './skillTypes';

// NOTE: SyntaxError and parse's options are not currently exposed.  They're
// not currently used.
export function parse(input: string): skillTypes.SkillEffect;
